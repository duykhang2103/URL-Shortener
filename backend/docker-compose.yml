services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - url-shortener-network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8321:8321"
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis # Add this line
      - REDIS_PORT=6379 # Add this line (optional, since 6379 is default)
    depends_on:
      - mongodb
      - redis
    networks:
      - url-shortener-network

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    networks:
      - url-shortener-network

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - url-shortener-network

networks:
  url-shortener-network:
    driver: bridge

volumes:
  redis_data:
